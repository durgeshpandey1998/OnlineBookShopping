@*@model IEnumerable<OnlineBookSHopping.Models.AssignRole>*@
@model IEnumerable<OnlineBookShopping.Models.AssignRole>

@{
    ViewData["Title"] = "RolePermission";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Role Permission</h2>
<div class="row">
    <div class="col-md-4">
        <form asp-action="RolePermission" method="post">
            <div class="form-group">
                <label class="control-label">Select User</label>
                <select class="form-control">
                    <option value="0">--Select User--</option>
                    @foreach (var item in ViewBag.role)
                    {
                        <option value="@item.Id">   @item.UserName</option>
                    }
                </select>
                <select class="form-control">
                    @foreach (var item in ViewBag.role)
                    {
                        <option value="@item.Id">   @item.UserName</option>

                    }
                </select>
                @* <span asp-validation-for="UserName" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label class="control-label">Select Role</label>
                <select class="form-control">
                    <option value="0">--Select Role--</option>

                    <option value="1"> Customer</option>
                    <option id="test" value="2">Student</option>

                </select>
                @* <span asp-validation-for="UserName" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <input type="submit" value="Assign Role" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


<div>
    <a asp-action="Index">Back to List</a>
</div>
